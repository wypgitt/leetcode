# 1104. Path In Zigzag Labelled Binary Tree - Go

## Go implementation notes
The Go version uses a `[]int` slice for the path and `math/bits.Len` to find the current level in constant time. The slice is reversed in place before returning.

## Go interview framing
In Go, explain both the algorithmic invariant and the concrete representation: slices for dense arrays/queues/stacks, maps for sparse lookup, structs for queue states or records, and channels or `sync` primitives for concurrency. Then walk through how the code mutates that representation while preserving the invariant.

## Core idea
Walk from the target label back to the root. At each level, mirror the zigzag label into the normal left-to-right label, divide by two to get the parent, and continue.

## Algorithm
1. The binary length of `label` tells us its level because level `d` contains labels `[2^(d-1), 2^d - 1]`.
2. For a zigzag row, the mirrored value in normal order is `level_start + level_end - label`.
3. The parent in the normal tree is that mirrored value divided by two. The formula `(level_start + level_end - label) // 2` gives the parent label as it appears on the previous zigzag row.
4. Collect labels while moving upward, then reverse the list.

## Data structure choice
Only an output list is required. The tree is implicit, so building nodes would waste memory.

## Correctness argument
Every step computes the unique parent of the current node in the underlying perfect binary tree, accounting for whether the current row is reversed. Repeating this until label zero reaches exactly the root path, and reversing changes the order from node-to-root into root-to-node.

## Complexity
There are `O(log label)` levels. Time is `O(log label)` and extra space is `O(log label)` for the returned path.

## Interview explanation
Start by naming the invariant: what state is being stored and why that state is sufficient. Then explain the transition or traversal in the same order the code executes. Finally, connect the stopping condition to the returned value. This keeps the explanation grounded in the code instead of sounding like a memorized trick.

## Tests and edge cases
- `label = 14` gives `[1, 3, 4, 14]`.
- `label = 1` returns `[1]`.
- Even-level targets such as `26` verify the mirror arithmetic.

## Possible improvements
The current solution is chosen for clarity and interview reliability. If constraints were much larger, the main improvement would be to reduce the stated bottleneck in the complexity section, while keeping the same correctness invariant.


